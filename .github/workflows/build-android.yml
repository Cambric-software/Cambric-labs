name: Build Android Application

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., 0.1.0)'
        required: true
        default: '0.1.0'
  push:
    tags:
      - 'v*'

jobs:
  build-android:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: |
          cd frontend
          npm ci

      - name: Install Capacitor
        run: |
          cd frontend
          npm install @capacitor/core @capacitor/cli @capacitor/android

      - name: Build frontend
        run: |
          cd frontend
          npm run build

      - name: Initialize Capacitor
        run: |
          cd frontend
          npx cap init "CAMBRIC LABS" "com.cambric.labs" --web-dir=../docs

      - name: Add Android platform
        run: |
          cd frontend
          npx cap add android

      - name: Sync Capacitor to Android
        run: |
          cd frontend
          npx cap sync android

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v2

      - name: Build Android APK
        run: |
          cd frontend/android
          ./gradlew assembleRelease
        env:
          VERSION_NAME: ${{ github.event.inputs.version || github.ref_name }}

      - name: Build Android Debug APK
        run: |
          cd frontend/android
          ./gradlew assembleDebug

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: cambric-labs-android-release-${{ github.event.inputs.version || github.ref_name }}
          path: frontend/android/app/build/outputs/apk/release/*.apk
          retention-days: 30

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: cambric-labs-android-debug-${{ github.event.inputs.version || github.ref_name }}
          path: frontend/android/app/build/outputs/apk/debug/*.apk
          retention-days: 30

  create-release:
    needs: build-android
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          draft: false
          prerelease: false
          files: |
            artifacts/**/*.apk
          body: |
            ## CAMBRIC LABS Android v${{ github.ref_name }}
            
            ### Downloads
            - **Release APK**: For production use
            - **Debug APK**: For testing
            
            ### Features
            - Local neural network training (no internet required)
            - Create and train small neural networks
            - Visualize weights and gradients
            - Save and load projects locally
            
            ### System Requirements
            - Android 7.0 (API 24) or later
            - 2GB RAM minimum
            - 100MB storage space
            
            ### Installation
            1. Download the APK to your Android device
            2. Enable "Install from unknown sources" in settings
            3. Open the APK file to install
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
